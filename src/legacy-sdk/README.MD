`legacy-sdk` is in-progress migration of `weenspace/sdk` package. The end goal is to blend it's logic into standard Dashboard modules.

TODO

- Gradually move logic into src/auth
- Remove duplicated graphQL logic and types
